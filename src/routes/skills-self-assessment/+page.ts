// page.data.title convention: the full <title> text, read by the root layout
// for the tab title and for SharePicker. This page is otherwise static, so a
// load function exists only to carry it.
export function load() {
  return { title: 'Skills self-assessment — UK GDAD PCF' };
}
